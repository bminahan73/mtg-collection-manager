MTG Collection Manager

Minimal starter for managing an MTG collection.

Backend (basic):
- Express API that proxies Scryfall for card search
- Simple JSON-backed collection store (backend/data/collection.json)

To run backend:
1. cd backend
2. npm install
3. npm start

Goals: search & add cards, list collection, manage simple decks, label cards. Iterate from this scaffold.